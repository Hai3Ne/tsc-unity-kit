# Shared TSC Docs Guidance

Use this shared guidance when a platform adapter handles `$docs`.

1. Search `.tsc_workspace/docs/` recursively for the requested topic.
2. Prefer the most specific matching document.
3. Return only the relevant content needed for the question.
