# Shared TSC Examples Guidance

Use this shared guidance when a platform adapter handles `$code`.

1. Read `.shared/skills/examples/MonoBehaviourStyle.cs`.
2. Copy the project structure and TSC conventions.
3. Adapt only the parts required by the requested feature.
4. Check generated code against TSC Iron Rules before completion.
